@model WebMed_HeathCare_System.Models.InsurancePlanDetailsViewModel

@{
    ViewData["Title"] = Model.Plan.PlanName;
}

<div class="container mt-4">
    <a asp-action="Index" class="btn btn-secondary mb-3">&larr; Back to Plans</a>

    <div class="card shadow">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <h2 class="mb-0">@Model.Plan.PlanName</h2>
            <h3 class="mb-0 text-success">@(Model.Pricing != null ? Model.Pricing.Premium.ToString("N0") : "N/A") VND <small class="fs-6 text-light">/ @Model.Plan.DurationMonths months</small></h3>
        </div>
        <div class="card-body">
            <div class="row">
                <!-- Pricing & Network -->
                <div class="col-md-6 mb-4">
                    <h4 class="text-primary border-bottom pb-2">Pricing Information</h4>
                    <table class="table table-borderless">
                        <tbody>
                            <tr><th>Deductible:</th><td>@(Model.Pricing?.Deductible != null ? Model.Pricing.Deductible.Value.ToString("N0") + " VND" : "N/A")</td></tr>
                            <tr><th>Copay:</th><td>@(Model.Pricing?.Copay != null ? Model.Pricing.Copay.Value.ToString("N0") + " VND" : "N/A")</td></tr>
                        </tbody>
                    </table>
                </div>

                <!-- Coverage Details -->
                <div class="col-md-6 mb-4">
                    <h4 class="text-primary border-bottom pb-2">Coverage Highlights</h4>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item"><i class="bi bi-check-circle-fill text-success me-2"></i> @(Model.Coverage?.CoverageDetails ?? "No coverage details provided")</li>
                    </ul>
                </div>
            </div>

            <div class="row mt-2">
                <!-- Benefits Information -->
                <div class="col-12">
                    <h4 class="text-primary border-bottom pb-2">Additional Benefits</h4>
                    <div class="row">
                        @if (Model.Benefits != null && Model.Benefits.Any())
                        {
                            foreach (var benefit in Model.Benefits)
                            {
                                <div class="col-md-6 mb-2">
                                    <div class="d-flex align-items-center">
                                        <div class="bg-light rounded p-2 me-3"><i class="bi bi-star-fill text-warning"></i></div>
                                        <div>@benefit.BenefitName</div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="col-12"><p>No benefits listed.</p></div>
                        }
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer text-center">
            <a asp-action="Enroll" asp-route-planId="@Model.Plan.PlanId" class="btn btn-lg btn-primary">Enroll Now</a>
        </div>
    </div>
</div>
